import { assert } from "chai";

import * as Poptart from "Poptart/poptart";


describe("Poptart", function() {
	describe("#placeholder", function() {
		it("should return true cause i need a placeholder test.", function() {
			assert.isTrue(true);
		});
	});
});